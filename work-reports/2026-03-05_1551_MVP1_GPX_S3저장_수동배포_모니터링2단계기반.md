# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-05 15:51:20 (KST)
- 작업명: MVP1 GPX S3 저장 전환 + 수동 배포 정책 + 모니터링 2단계 기반 추가
- 브랜치: master
- 범위(커밋): 7c9cf95..(working)

## 1. 이번 작업의 목표

- GPX 원문을 운영에서 S3에 저장하고 DB는 key만 보관하는 구조를 적용한다.
- GitHub Actions 배포 트리거를 수동 승인 중심으로 고정한다.
- Prometheus/Loki/Grafana 2단계 도입용 실행 파일/문서를 레포에 추가한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - `Course.gpxObjectKey` 컬럼 추가
  - GPX 저장 전략 컴포넌트 추가
    - `CourseGpxStorage` 인터페이스
    - `DbCourseGpxStorage` (기본/local)
    - `S3CourseGpxStorage` + S3 설정(`CourseGpxS3Config`, `CourseGpxS3Properties`)
  - Course 생성/조회 흐름에서 GPX 저장/조회 로직을 storage 전략으로 이관
  - 의존성 추가:
    - `spring-boot-starter-actuator`
    - `micrometer-registry-prometheus`
    - `aws-sdk-v2 s3`
  - 설정 추가:
    - `course.gpx.storage.mode`
    - `course.gpx.s3.*`
    - actuator prometheus 노출 설정
- CI/CD:
  - `deploy-prod.yml`을 `workflow_dispatch` 전용으로 변경
  - 수동 입력값(`deploy_backend`, `deploy_frontend`) 추가
  - 입력 검증 step 추가(둘 다 false 방지)
  - frontend 배포 여부에 따라 SCP step 분기
- Deploy/Monitoring:
  - `deploy/monitoring/docker-compose.monitoring.yml` 추가
  - Prometheus/Loki/Promtail 설정 파일 추가
  - monitoring runtime 디렉터리 `.gitkeep` 추가
  - `deploy/env/.env.prod.example`에 GPX S3 관련 변수 추가
- Docs:
  - `docs/deploy-mvp1.md` 수동 배포/GPX S3 반영
  - `docs/cicd-secrets.md` 환경승인/GPX S3 변수 반영
  - `docs/github-actions-usage.md` 신규
  - `docs/monitoring-phase2.md` 신규
  - `docs/mvp1_strategy_options.md`에 GPX 저장/모니터링 전략 장단점 추가
  - `설계/10_데이터모델_스키마.md`에 `gpx_object_key` 반영
  - `설계/11_API_명세.md`에 GPX 저장 정책(내부) 주석 반영

## 3. 변경된 API / 정책(있으면)

- 외부 API 계약 변경 없음
  - `GET /api/v1/courses/{id}/gpx` 응답 형식 동일
- 내부 저장 정책 변경
  - 운영 기본: `S3 본문 + DB(gpx_object_key)`
  - 로컬 기본: `DB(gpx_data)`
- 배포 정책 변경
  - `deploy-prod.yml`: 자동 push 트리거 제거, 수동 실행 전용

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd backend && ./gradlew test`
- 결과:
  - PASS

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- S3 모드에서는 EC2 IAM Role 또는 AWS 자격증명 구성이 반드시 필요하다.
- 모니터링 스택은 2단계(옵션) 파일 추가 상태이며, 운영 포트 접근제어(Security Group) 설정이 필요하다.

## 7. 다음 작업(Next)

- EC2 IAM Role(S3 Put/Get) + 버킷 정책 연결
- production 환경 승인 룰(Required reviewers) 설정
- 실제 배포 후 GPX 업로드/다운로드 및 모니터링 스택 기동 검증

# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-07 00:55:00 (KST)
- 작업명: 카카오 로그인 401 디버깅 및 토큰 우선순위 수정
- 브랜치: master
- 범위(커밋): 450d562..(next)

## 1. 이번 작업의 목표

- 카카오 로그인 후에도 `/profile`에서 401이 나며 자동 로그아웃되는 원인을 프론트/백엔드/구번들 관점에서 분리 진단한다.
- same-origin `/api/auth/me` 경로를 기준으로 로그인 상태 확인을 안정화한다.
- dev 콘솔의 CSP/구번들 혼선을 실제 서빙 번들 기준으로 점검한다.

## 2. 완료된 작업(Deliverables)

- Web:
  - `frontend/lib/auth.ts`
    - 저장된 토큰 선택 시 `localStorage`보다 `cookie`를 우선 사용하도록 수정
  - `frontend/components/auth/auth-provider.tsx`
    - `/api/auth/me` 확인 요청 시 `Authorization: Bearer <stored>` 헤더를 함께 전달하도록 수정
    - 401 계열 실패 시 "저장된 로그인 정보가 만료되었어요" 메시지로 구분
- Debug Findings:
  - fresh dev bundle 기준 현재 서빙되는 프론트 코드는 `fetch('/api/auth/me')`를 사용함
  - fresh dev HTML 기준 `va.vercel-scripts.com` 스크립트는 직접 주입되지 않음
  - `GET /api/auth/me`는 무인증 401, 유효 Bearer 전달 시 200으로 동작 확인

## 3. 변경된 API / 정책(있으면)

- API 경로 변경 없음
- 인증 확인 정책 보강:
  - 프론트는 same-origin `/api/auth/me`에 Bearer 헤더를 같이 전달해 cookie-only 의존을 줄임
  - 저장 토큰 선택 시 cookie를 우선시해 로그인 직후 fresh cookie가 stale localStorage에 가려지지 않도록 함

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd frontend && npm run check`
  - `cd backend && ./gradlew test --no-daemon --console=plain`
  - `curl -i http://localhost:3000/api/auth/me`
  - `curl -i http://localhost:3000/api/auth/me -H "Authorization: Bearer <local-test-jwt>"`
  - `curl http://localhost:3000/profile`
  - `python` 스크립트로 local test JWT 발급 후 `/api/v1/auth/me` 200 확인
- 결과:
  - frontend lint/typecheck/build 통과
  - backend test 통과
  - `/api/auth/me` 무인증 401 확인
  - `/api/auth/me` Bearer 전달 시 200 확인
  - fresh dev HTML에 `http://localhost:8080/api/auth/me` 직접 호출 문자열 없음 확인
  - fresh dev chunk에 `fetch('/api/auth/me')` + `Authorization: Bearer ${stored}` 반영 확인

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- `파일 시스템을 추가할 수 없습니다. <illegal path>` 메시지는 앱 인증 로직보다 Chrome DevTools Workspace/브라우저 확장 측 이슈 가능성이 높다.
- 사용자가 오래 열어둔 탭에서는 구번들이 남아 있을 수 있어, 브라우저 탭 재오픈/강력 새로고침이 여전히 권장된다.
- 이번 수정의 핵심 원인 후보는 "fresh cookie보다 stale localStorage 토큰이 먼저 선택되던 문제"였다.

## 7. 다음 작업(Next)

- 사용자 브라우저에서 실제 카카오 로그인 1회 재검증
- 필요 시 불법 path 메시지의 브라우저 확장/워크스페이스 원인을 추가 분리

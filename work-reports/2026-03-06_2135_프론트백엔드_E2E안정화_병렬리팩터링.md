# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-06 21:35:00 (KST)
- 작업명: 프론트/백엔드 E2E 안정화 병렬 리팩터링
- 브랜치: master
- 범위(커밋): 80c1187..403cf9e

## 1. 이번 작업의 목표

- 프론트와 백엔드를 병렬로 점검해 실제 사용자 플로우 기준 E2E 안정성을 높인다.
- 카카오 로그인, 코스 상세/채팅 동적 라우팅, 지도 렌더링, 라이딩 저장 → 코스화 흐름의 취약점을 보완한다.
- 변경 후 빌드/테스트/스모크를 다시 통과시켜 배포 전 단계 신뢰도를 높인다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - `RidingService` 입력 검증 강화 (null/path 개수/좌표 범위)
  - 라이딩/코스 생성 시 geometry SRID를 4326으로 고정
  - `CourseCreateRequest`, `RidingCreateRequest` 에 `lng` JSON alias 허용
  - `sourceType=RIDING` 레거시 입력을 `UGC`로 호환 처리 유지
  - 회귀 테스트 추가:
    - `AuthServiceTest`
    - `CourseServiceFromRidingTest`
    - `RidingServiceTest`
- Frontend:
  - 동적 라우트 페이지를 `use(params)` 대신 `useParams()` 기반으로 정리
  - 카카오 callback 중복 처리 방지(ref 기반 시도 중복 차단)
  - `MapView`를 단일 SDK 로더/맵 인스턴스 재사용 구조로 리팩터링
  - 지도 overlay 정리/resize observer 적용으로 라우트 전환 및 화면 갱신 안정화
  - `apiFetch`에 timeout 및 네트워크 실패 메시지 추가
  - 라이딩 저장/코스화 호출에 인증 토큰 전달 가능하도록 정리
- Runtime/E2E:
  - `/profile`, `/course/1`, `/course/1/guide`, `/meetups/1/chat` 웹 라우트 스모크 확인
  - `POST /api/v1/ridings` → `POST /api/v1/courses/from-riding` → `GET /api/v1/courses/{id}` 흐름 재검증
  - `lng` alias 입력과 `sourceType=RIDING` 호환 처리까지 실제 API로 확인

## 3. 변경된 API / 정책(있으면)

- API 경로/응답 스키마 변경 없음
- 요청 호환성 확대:
  - 좌표 입력에서 `lon` 외에 `lng`도 허용
  - `from-riding` 시 `sourceType=RIDING` 입력도 허용
- 프론트 API 클라이언트 정책 보강:
  - 기본 timeout 15초
  - 네트워크/타임아웃 오류 메시지 명확화

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd backend && ./gradlew test --no-daemon --console=plain`
  - `cd frontend && npm run check`
  - `curl -sS http://localhost:8080/api/v1/health`
  - `curl -sS http://localhost:3000/api/health`
  - `curl -s -o /tmp/profile_after.html -w '%{http_code}' http://localhost:3000/profile`
  - `curl -s -o /tmp/course_detail_after.html -w '%{http_code}' http://localhost:3000/course/1`
  - `curl -s -o /tmp/guide.html -w '%{http_code}' http://localhost:3000/course/1/guide`
  - `curl -s -o /tmp/chat.html -w '%{http_code}' http://localhost:3000/meetups/1/chat`
  - `curl -sS -X POST http://localhost:8080/api/v1/ridings ...`
  - `curl -sS -X POST http://localhost:8080/api/v1/courses/from-riding ...`
  - `curl -sS http://localhost:8080/api/v1/courses/{id}`
  - `curl -sS -X POST http://localhost:8080/api/v1/courses ...` (`lng` alias 스모크)
- 결과:
  - backend test 통과
  - frontend lint/typecheck/build 통과
  - 주요 웹 라우트 200 응답 확인
  - health endpoints 정상 응답
  - 라이딩 저장 → 코스 생성 → 코스 상세 조회 정상 동작
  - `lng` alias 및 `sourceType=RIDING` 호환 입력 정상 동작

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- 카카오 로그인 최종 성공 여부는 여전히 카카오 개발자 콘솔 설정 상태에 영향받는다.
- 작업 트리에는 본 작업과 무관한 미정리 파일(`OMX_GUIDE.md`, `backend/build.gradle`, `backend/docker-compose.local.yml`, 기타 docs/png`)이 남아 있다.
- 프론트 E2E 자동화(Playwright 등)는 아직 도입하지 않았고 현재는 빌드 + 스모크 + API 검증 중심이다.

## 7. 다음 작업(Next)

- 카카오 콘솔 설정 반영 후 실제 로그인 E2E 재검증
- 코스 상세 상단 레이아웃/지도 배치에 대한 시각 회귀 확인
- 필요 시 웹 E2E 자동화(Playwright) 최소 시나리오 도입 검토

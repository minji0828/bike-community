# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-11 16:55:24 (KST)
- 작업명: Vercel 배포 락파일 정리 및 AWS env 로딩 정렬
- 브랜치: master
- 범위(커밋): 2f4d0f2..4688db1

## 1. 이번 작업의 목표

- Vercel 빌드를 막는 패키지 락파일 불일치를 제거한다.
- AWS 홈테스트 compose가 실제 `.env`를 읽도록 정렬해 EC2 재기동 오류를 줄인다.

## 2. 완료된 작업(Deliverables)

- Frontend:
  - `pnpm-lock.yaml` 제거로 Vercel이 `npm` 기준으로 설치하도록 정리
- Backend:
  - 직접 코드 변경 없음
- Docs:
  - 직접 문서 변경 없음
- Deploy:
  - `deploy/aws/docker-compose.home-test.yml`의 `env_file`을 `.env` 기준으로 수정

## 3. 변경된 API / 정책(있으면)

- 공개 API 변경 없음
- 배포 동작 변경:
  - 프론트 배포는 `npm` 기준 잠금 파일 체계로 정리
  - EC2 compose는 더 이상 예시 env 파일을 런타임으로 읽지 않음

## 4. 검증(Verification)

- 실행한 커맨드:
  - `git diff --check`
  - `cd frontend && npm run lint`
  - `cd frontend && npm run typecheck`
  - `cd frontend && npm run build`
- 결과:
  - 형식 오류 없음
  - lint 통과
  - typecheck 통과
  - production build 통과

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식(권장): Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 57%

## 6. 리스크/이슈/메모

- 카카오 redirect URI와 CORS는 실제 Vercel 도메인 확정 후 한 번 더 갱신해야 한다.
- EC2 backend는 테스트 편의를 위해 `8080`을 직접 노출 중이므로 장기 운영용 구성은 아니다.

## 7. 다음 작업(Next)

- Vercel preview 배포를 재시도해 실제 도메인을 확보한다.
- 확보된 도메인 기준으로 EC2 `.env`와 Kakao Developers 설정을 최종 고정한다.
- 외부 의뢰자 테스트 체크리스트를 실제 URL 기준으로 마감한다.

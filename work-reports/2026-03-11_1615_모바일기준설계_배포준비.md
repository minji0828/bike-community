# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-11 16:15:00 (KST)
- 작업명: 모바일 기준 설계 재정렬 및 공개 테스트 배포 준비
- 브랜치: master
- 범위(커밋): 25b156c..16e8b49

## 1. 이번 작업의 목표

- 제품의 1차 클라이언트를 모바일 기준으로 다시 고정한다.
- 도메인 정책을 서술형 + 표형 정책코드로 정리하고, 실제 코드에 정책 ID를 연결한다.
- 외부 테스터가 집에서 공개 URL로 테스트할 수 있도록 Vercel/Render 배포 준비를 한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - 코스/라이딩/공유/컬렉션/하이라이트/모임/인증 핵심 로직에 정책 ID 주석 연결
  - CORS exact origin + origin pattern 지원 추가
  - Render 환경에서 DB URL을 조립하는 entrypoint 추가
  - `PORT` 기반 서버 포트 바인딩 지원
- Mobile:
  - 모바일 1차 클라이언트 기준 문서 추가
  - 모바일 화면 구조, GPS/인증/API 원칙 정리
- Docs:
  - 도메인 정책 문서 2종 추가
  - 화면 설계, 디자인 토큰, 백오피스 운영 명세 추가
  - 인증/OIDC, GPS, API, 스키마, 배포, 기술스택 문서 최신화
  - Render/Vercel 배포 전략 문서 추가
  - 외부 테스터 전체 계획 및 최소 행동 체크리스트 추가

## 3. 변경된 API / 정책(있으면)

- 정책:
  - `RID-P`, `CRS-P`, `SHR-P`, `COL-P`, `HLT-P`, `MTP-P`, `VAL-P`, `AUTH-P` 정책코드 체계 추가
- 배포/운영:
  - 프론트 웹은 Vercel, 백엔드는 EC2 전 단계로 Render 우선 전략 문서화
- 설정:
  - `APP_CORS_ALLOWED_ORIGIN_PATTERNS` 추가
  - `POI_FETCH_ONSTARTUP`, `COURSE_SEED_GPX_ONSTARTUP` env 기반 제어 추가
  - `server.port=${PORT:8080}` 추가

## 4. 검증(Verification)

- 실행한 커맨드:
  - `git diff --check`
  - `cd backend && ./gradlew test --tests "com.bikeoasis.domain.riding.service.RidingServiceTest" --tests "com.bikeoasis.domain.course.service.CourseServiceFromRidingTest" --tests "com.bikeoasis.global.config.WriteApiSecurityTest"`
  - `cd frontend && npm run lint`
  - `cd frontend && npm run typecheck`
- 결과:
  - `git diff --check` 통과
  - 백엔드 관련 테스트 통과
  - 프론트 `lint` 통과
  - 프론트 `typecheck` 통과
  - 참고: `WriteApiSecurityTest`의 `@MockBean` deprecation warning은 기존 경고로 유지됨

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식(권장): Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 50%

## 6. 리스크/이슈/메모

- 모바일 1차 클라이언트는 문서로 고정됐지만 실제 `frontend/mobile` 소스는 아직 없다.
- Render Blueprint는 repo와 시크릿 입력이 있어야 실제 생성 가능하다.
- 카카오 redirect URI와 Vercel/Render 도메인이 실제 배포 후 맞춰져야 홈테스트가 가능하다.

## 7. 다음 작업(Next)

- `frontend/mobile` 부트스트랩
- Render 백엔드 + Vercel 웹 실제 배포
- 배포 URL 기준 카카오 redirect URI 확정
- 외부 테스터 공개 URL smoke test

# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-05 14:55:00 (KST)
- 작업명: M5 백오피스 최소 API 구현(태그/경고/코스 메타데이터 재계산)
- 브랜치: master
- 범위(커밋): 4d3ab4a..f74cfc7

## 1. 이번 작업의 목표

- 운영자가 코스 운영 데이터를 직접 제어할 수 있도록 M5 최소 API를 구현한다.
- 태그/경고/메타데이터 재계산을 서버 API로 제공한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - `CourseAdminController`, `CourseAdminService` 신규 추가
  - 태그 사전 API 구현:
    - 조회/생성/수정/비활성화
  - 코스 경고 API 구현:
    - 조회/생성/삭제
  - 코스 메타데이터 재계산 API 구현:
    - distanceKm / estimatedDurationMin / loop / bbox / toiletCount 재계산
  - 관리자 인증: `X-Admin-Key` 기반 기존 `AdminKeyAuthService` 재사용
  - 테스트 추가: `CourseAdminServiceTest`
- Mobile:
  - 직접 수정 없음(관리자 UI/도구 연동은 후속)
- Docs:
  - `설계/11_API_명세.md` Admin/Backoffice API 구간을 실제 경로로 동기화

## 3. 변경된 API / 정책(있으면)

- Admin API(신규/구체화):
  - `GET /api/v1/admin/tags`
  - `POST /api/v1/admin/tags`
  - `PATCH /api/v1/admin/tags/{tagId}`
  - `DELETE /api/v1/admin/tags/{tagId}` (비활성화)
  - `GET /api/v1/admin/courses/{courseId}/warnings`
  - `POST /api/v1/admin/courses/{courseId}/warnings`
  - `DELETE /api/v1/admin/warnings/{warningId}`
  - `POST /api/v1/admin/courses/{courseId}/metadata/recalculate`

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd backend && ./gradlew test`
- 결과:
  - BUILD SUCCESSFUL

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 상태(추정):
  - M0 Done, M1 Done, M2 Partial, M3 Partial, M4 Done, M5 Done, M6 Done, M7 Done
- 이번 보고 시점 진척도: 87.5% (7.0/8)

## 6. 리스크/이슈/메모

- metadata 재계산의 toiletCount는 현재 즉시 계산 방식이므로 코스 수 증가 시 배치/큐 분리가 필요할 수 있다.
- Admin API는 키 기반이므로 운영 키 로테이션/감사로그 체계는 후속 과제다.

## 7. 다음 작업(Next)

- Admin API 스모크(실데이터) 검증 및 Swagger 예시 보강
- M2 featured 운영 자동화(최소 10개 seed/관리 흐름) 정리

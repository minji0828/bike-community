# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-06 18:48:13 (KST)
- 작업명: 웹 프리디플로이 정비 및 로컬 배포 검증
- 브랜치: master
- 범위(커밋): 6fc3b03..ab9315a

## 1. 이번 작업의 목표

- 현재 웹/백엔드 구성을 로컬에서 배포 전 단계까지 검증 가능한 수준으로 끌어올린다.
- 프론트 품질 게이트(lint/typecheck/build), 운영용 프론트 배포 방식, 백엔드 운영 안전 설정을 정리한다.
- 실제 런타임 스모크 테스트까지 수행해 배포 직전 체크리스트를 문서화한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - CORS를 `allowedOrigins("*")`에서 `APP_CORS_ALLOWED_ORIGINS` 기반 allowlist로 변경
  - GPX 입력에 최대 XML 크기/트랙포인트 수 제한 추가
  - `application-prod.yml`에서 `ddl-auto: none`, `springdoc` 비활성화 반영
- Mobile/Web:
  - 프론트에 ESLint/타입체크/빌드 품질 게이트 추가 (`npm run check`)
  - Next.js 빌드를 `--webpack` + `output: standalone` 기준으로 정리
  - 프론트 `/api/health` 헬스체크 엔드포인트 추가
  - 보안 헤더(CSP, X-Frame-Options, Referrer-Policy 등) 추가
  - 프로덕션용 `frontend/Dockerfile` / `.dockerignore` 추가
- Deploy/CI:
  - `ci-frontend.yml` 를 `frontend/mobile` 기준에서 현재 `frontend/` Next.js 기준으로 수정
  - `deploy-prod.yml` 을 Next 웹 프론트 소스 업로드/검증 흐름으로 수정
  - `deploy/docker-compose.prod.yml` 에 `frontend-web` 서비스 추가
  - Nginx가 정적 dist 대신 `frontend-web:3000` 으로 프록시하도록 변경
  - `deploy/scripts/deploy_frontend.sh` 를 헬스체크 포함 Docker 기반 프론트 배포 스크립트로 교체
  - 운영 env 예시에 웹 프론트/카카오/CORS 관련 값 추가
- Docs:
  - `docs/predeploy-checklist.md` 추가
  - `docs/local-dev-runbook.md` 에 프론트 품질 게이트/지도 키/헬스체크 내용 보강

## 3. 변경된 API / 정책(있으면)

- 백엔드 API 엔드포인트 계약 변경 없음
- 프론트 내부 헬스체크 라우트 추가:
  - `GET /api/health` (Next.js)
- 운영 정책 변경:
  - 백엔드 CORS는 환경변수 allowlist 기반
  - 운영 프로필 DB 스키마 자동 변경 비활성화 (`ddl-auto: none`)
  - 운영 프로필에서 SpringDoc 비활성화

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd frontend && npm run lint`
  - `cd frontend && npm run check`
  - `cd backend && ./gradlew test`
  - `cd backend && GRADLE_USER_HOME=/tmp/gradle-home ./gradlew --no-daemon --project-cache-dir /tmp/gradle-project-cache -Dorg.gradle.vfs.watch=false clean build`
  - `cd frontend && PORT=3001 npm run start`
  - `curl -sS http://localhost:3001/api/health`
  - `curl -I -sS http://localhost:3001/`
  - `cd backend && set -a && source .env && set +a && SERVER_PORT=8082 GRADLE_USER_HOME=/tmp/gradle-home ./gradlew --project-cache-dir /tmp/gradle-project-cache -Dorg.gradle.vfs.watch=false bootRun`
  - `curl -sS http://localhost:8082/api/v1/health`
  - `curl -sS http://localhost:8082/api/v1/courses/featured`
  - `bash -n deploy/scripts/deploy_frontend.sh deploy/scripts/deploy_backend.sh`
  - `cp deploy/env/.env.prod.example deploy/env/.env.prod && docker compose -f deploy/docker-compose.prod.yml config ... && rm deploy/env/.env.prod`
- 결과:
  - 프론트 lint/typecheck/build 통과
  - 백엔드 test/clean build 통과
  - 프론트 production 서버 기동 및 `/api/health` 응답 확인
  - 프론트 응답 헤더에서 CSP/보안 헤더 반영 확인
  - 백엔드 최신 코드로 8082 포트 기동 후 health/featured 응답 확인
  - 배포 스크립트 bash 문법 확인 완료
  - 운영 docker-compose 구성이 예시 env 기준으로 유효함을 확인

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- 공개 운영 전 기준으로는 아직 추가 하드닝 여지가 있음:
  - JWT를 `localStorage` 대신 HttpOnly 쿠키로 전환
  - 익명 생성 API rate limit
  - GPX 파서 streaming/추가 제한 강화
- 로컬 작업 트리에는 이번 작업과 무관한 미커밋 파일(`OMX_GUIDE.md`, `backend/build.gradle`, `backend/docker-compose.local.yml`, `docs/omx-command-guide-bike-project.md`, `제목 없음.png`)이 남아 있음.

## 7. 다음 작업(Next)

- staging/실서버 env를 채운 `deploy/env/.env.prod` 실제값으로 1회 배포 리허설
- 카카오 로그인 토큰 저장 방식을 HttpOnly 쿠키 기반으로 전환 검토
- 익명 생성 API 보호(visibility 정책/rate limit) 추가

# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-05 15:40:00 (KST)
- 작업명: MVP1 배포 스택/CI-CD/운영 문서 일괄 구축
- 브랜치: master
- 범위(커밋): 07af528..ee4d5ac

## 1. 이번 작업의 목표

- MVP1(프론트+백엔드) 실배포를 위한 인프라/자동화/운영 문서를 레포에 구축한다.
- 전략 선택 시 장단점 문서화를 규칙으로 고정한다.

## 2. 완료된 작업(Deliverables)

- Backend/Infra:
  - `backend/Dockerfile`, `.dockerignore` 추가
  - `application-prod.yml` 추가
  - `deploy/docker-compose.prod.yml` 추가 (backend-blue/green + nginx)
  - Nginx 설정 추가 (`deploy/nginx/*`)
  - 배포 스크립트 추가:
    - `deploy/scripts/deploy_backend.sh` (blue/green 전환)
    - `deploy/scripts/deploy_frontend.sh`
    - `deploy/scripts/enable_https.sh`
  - prod env 템플릿 추가: `deploy/env/.env.prod.example`
- CI/CD:
  - `.github/workflows/ci-backend.yml`
  - `.github/workflows/ci-frontend.yml`
  - `.github/workflows/deploy-prod.yml`
- Docs:
  - `docs/mvp1_strategy_options.md` (전략별 장단점 상세)
  - `docs/deploy-mvp1.md`
  - `docs/rollback-mvp1.md`
  - `docs/e2e-mvp1-checklist.md`
  - `docs/cicd-secrets.md`
  - `docs/README.md`
  - `설계/11_API_명세.md` 동기화(health/riding 응답)
  - `AGENTS.md`에 전략 옵션 장단점 문서화 규칙 추가
- 기타:
  - `.gitignore`에 `deploy/env/.env.prod` 추가

## 3. 변경된 API / 정책(있으면)

- 신규 API 문서화: `GET /api/v1/health`
- Riding 응답 정책 문서: ApiResponse 래핑 기본 + legacy 호환
- 운영 정책 문서화:
  - 전략 질문 시 장단점 문서를 함께 남기는 규칙 추가

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd backend && ./gradlew test`
  - `cd frontend/mobile && npm run typecheck`
  - `cd frontend/mobile && npm run build:web`
- 결과:
  - backend test: PASS
  - frontend typecheck: PASS
  - frontend build:web: FAIL (로컬 Node24 환경에서 `lightningcss` native 모듈 로드 실패)
    - CI 워크플로는 Node20 기준으로 구성되어 있어 실제 CI 환경 재검증이 필요

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- 실배포 전 EC2/RDS/도메인/Secrets 입력이 완료되어야 deploy workflow 실행 가능하다.
- HTTPS는 인증서 발급/도메인 연결 완료 후 enable 스크립트 적용이 필요하다.

## 7. 다음 작업(Next)

- GitHub Secrets 설정 + EC2 서버 초기 세팅
- deploy-prod workflow 수동 실행 후 실제 smoke/E2E 진행
- Node20 환경에서 frontend web 빌드 성공 여부 확정

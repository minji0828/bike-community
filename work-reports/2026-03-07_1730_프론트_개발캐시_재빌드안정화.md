# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-07 17:30:37 (KST)
- 작업명: 프론트 개발 캐시/재빌드 문제로 인한 채팅 웹소켓 디버깅 안정화
- 브랜치: master
- 범위(커밋): b4ae6f4..504bacb

## 1. 이번 작업의 목표

- 모임 채팅에서 계속 보이던 `SimpleStompClient.disconnect` / `DISCONNECT` 에러의 실제 원인이 코드인지, 프론트 개발 서버 번들 캐시인지 분리한다.
- 브라우저가 최신 `frontend/lib/stomp.ts` 코드를 실제로 받도록 개발 서버 재빌드 경로를 안정화한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - 변경 없음
- Mobile:
  - 변경 없음
- Frontend:
  - `frontend/next.config.mjs`에 개발 환경 전용 webpack 설정 추가
    - `config.cache = false`
    - `watchOptions.poll = 1000`
    - `watchOptions.aggregateTimeout = 300`
  - `.next` 캐시를 비우고 `next dev --webpack`를 재시작한 뒤, 실제 서빙 chunk가 최신 `stomp.ts` 방어 로직을 포함하는지 재검증
- Docs:
  - 본 작업 보고서 추가

## 3. 변경된 API / 정책(있으면)

- API 변경 없음
- 운영 정책 변경 없음
- 개발 환경에서만 webpack 파일 캐시를 끄고 polling 감시를 사용하도록 조정

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd frontend && npm run check`
  - `curl http://localhost:3000/meetups/1/chat`
  - `curl http://localhost:3000/_next/static/chunks/app/meetups/%5BmeetupId%5D/chat/page.js`
- 결과:
  - lint / typecheck / build 통과
  - 브라우저에 서빙되는 chunk에서 이전의 무방어 `disconnect()` 구현이 아니라, `readyState` 체크 및 `try/catch`가 포함된 최신 `stomp.ts` 코드가 확인됨
  - `MeetupChat` chunk에서도 `cancelled`, `connectTimer`, `toFriendlyChatError` 등 최신 방어 로직 반영 확인

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식(권장): Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- 이번 이슈의 핵심은 채팅 코드 자체만이 아니라, Next.js dev 서버가 한동안 구버전 browser chunk를 서빙하던 점이었다.
- 따라서 사용자가 본 `lib/stomp.ts (129:17) @ SimpleStompClient.disconnect` 스택은 최신 소스와 불일치했고, 실제로 서빙 chunk를 직접 확인해서 원인을 분리했다.
- WSL/Windows 마운트 환경에서 파일 변경 감지가 늦거나 webpack 캐시가 꼬일 때 유사 증상이 재발할 수 있어 dev 전용 polling/caching 완화를 넣었다.

## 7. 다음 작업(Next)

- 실제 브라우저에서 `/meetups/[meetupId]/chat` 진입 후 WebSocket handshake 상태(101/403/failed)를 다시 확인한다.
- 여전히 채팅이 안 되면 이제는 stale bundle이 아니라 실제 인증/참가자 권한/WS handshake 문제로 좁혀 디버깅한다.

# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-04 14:07 (KST)
- 작업명: 프론트/모바일 추가 + 프로젝트 구조 분리 + 코스 도메인 + 설계(SSOT) 문서 도입
- 브랜치: master
- 범위(커밋): 9e4b25c..804c4a2

## 1. 이번 작업의 목표

- 레포를 "백엔드 + 프론트(문서/모바일)"로 분리해 개발 구조를 고정한다.
- 코스(Course) 도메인을 도입해 Featured/상세/공유/GPX 등 MVP 핵심 흐름을 서버에 만든다.
- 설계 문서를 `설계/` 폴더로 도입해 SSOT(문서가 단일 진실원천) 운영을 시작한다.

## 2. 완료된 작업(Deliverables)

- Repo 구조:
  - `backend/` / `frontend/` 디렉토리로 분리(Gradle/Java는 backend로 이동)
  - 에이전트/빌드 규칙 문서 `AGENTS.md` 추가

- Backend:
  - Course Catalog 도메인 추가
    - 코스 생성(JSON/GPX), 상세 조회, Featured 조회
    - 코스 shareId 발급 + 공개(shareId) 조회
  - Course seed loader(초기 featured 코스 시드) 추가
  - 웹 설정(`WebConfig`) 및 로컬 설정 예시(`application-local.yml.example`) 추가

- Frontend docs:
  - `frontend/docs/`에 화면/아키텍처/API 매핑/개발 환경 문서 추가

- Mobile (React Native / Expo):
  - `frontend/mobile/` 앱 스캐폴딩 추가
  - 지도/라이딩/설정 화면 골격 + API client + 거리 계산/다운샘플 유틸 추가
  - 코스 리스트/상세/따라가기 화면 추가

- 설계(SSOT) 문서:
  - `설계/00_START_HERE.md` 포함, 설계 문서맵/정책/API/스키마/개발계획 문서 일괄 도입
  - 샘플 자료(GPX/PDF 등) 포함

## 3. 변경된 API / 정책(있으면)

- Course:
  - `POST /api/v1/courses`
  - `POST /api/v1/courses/gpx`
  - `GET /api/v1/courses/{courseId}`
  - `GET /api/v1/courses/{courseId}/gpx`
  - `GET /api/v1/courses/featured`
  - `POST /api/v1/courses/{courseId}/share`
  - `GET /api/v1/courses/public/{shareId}`
  - `GET /api/v1/courses/public/{shareId}/gpx`

## 4. 검증(Verification)

- 당시 실행 로그는 커밋에 남지 않아 확인 불가
- 구조 분리 이후 기준 커맨드:
  - Backend: `backend/./gradlew.bat test`
  - Mobile: `frontend/mobile/npx tsc --noEmit`

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0

현황(해당 시점 기준, 사후 정리):

- M0 서버 베이스라인: Done
- M1 Course Catalog: Done
- M2 Featured 운영: Done
- M3 RN 최소 기능(Featured -> 상세 -> 따라가기): Done
- M4 UGC 코스 생성(from-riding) + GPS 문서: Todo
- M5 Backoffice 최소 API: Todo
- M6 인증 + 코스 댓글(MVP2): Todo
- M7 코스 모임 + 임시 채팅(MVP3): Todo

진척도(%):

- Done 4개 = 4
- 4 / 8 = 50%  -> 50%

## 6. 리스크/이슈/메모

- `frontend/docs/.env`에 `EXPO_PUBLIC_...` 형태의 키가 커밋됨
  - 의도적으로 public로 노출되는 키이지만, 운영 환경에서는 도메인/번들 제한 등으로 보호 필요
- (당시) Expo 실행 로그(`*.log`)가 트래킹될 위험이 생김 -> 이후 레포 hygiene로 정리 필요

## 7. 다음 작업(Next)

- 라이딩 -> 코스 생성(from-riding) 플로우(M4) 연결
- MVP3 모임 REST/정원 동시성(M7 일부) 착수
- MVP2 인증/댓글(M6) 착수

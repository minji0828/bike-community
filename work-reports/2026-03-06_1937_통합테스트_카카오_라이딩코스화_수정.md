# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-06 19:37:00 (KST)
- 작업명: 통합 테스트 재수행 및 카카오/라이딩 코스화 오류 보완
- 브랜치: master
- 범위(커밋): 9a18893..d265bb9

## 1. 이번 작업의 목표

- 사용자 요청(카카오 로그인 에러, 통합 테스트 확인)에 맞춰 실제 런타임 기준으로 문제를 재현/검증한다.
- 라이딩 기록 저장 후 코스 생성이 실패하던 흐름을 실제 API 호출로 확인하고 수정한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - `CourseService.parseSourceType()`에 `RIDING` 입력 호환 처리 추가 (`RIDING` -> `UGC` 매핑)
  - `POST /api/v1/courses/from-riding` 레거시 sourceType 입력도 정상 처리되도록 보완
- Mobile/Web:
  - `frontend/app/ride/page.tsx`에서 라이딩 코스 생성 요청 sourceType을 `RIDING` -> `UGC`로 수정
  - 로컬 dev 런타임 안정화를 위해 `frontend/package.json` dev 스크립트를 `next dev --webpack`으로 유지
- Docs:
  - `docs/local-dev-runbook.md`에 카카오 `KOE004` 점검 항목 추가

## 3. 변경된 API / 정책(있으면)

- API 경로/응답 스키마 변경 없음
- 동작 호환성 개선:
  - `sourceType=RIDING` 요청이 들어와도 서버가 `UGC`로 처리 가능

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd backend && ./gradlew test --no-daemon --console=plain`
  - `cd frontend && npm run check`
  - `curl -sS http://localhost:8080/api/v1/health`
  - `curl -sS http://localhost:3000/api/health`
  - `curl -s -o /tmp/profile.html -w '%{http_code}' http://localhost:3000/profile`
  - `curl -sS 'https://kauth.kakao.com/oauth/authorize?...'` (카카오 authorize 화면 응답 확인)
  - `curl -sS -X POST http://localhost:8080/api/v1/auth/kakao ...` (fake code 스모크)
  - `curl -sS -X POST http://localhost:8080/api/v1/ridings ...`
  - `curl -sS -X POST http://localhost:8080/api/v1/courses/from-riding ...` (sourceType=RIDING)
  - `curl -sS http://localhost:8080/api/v1/courses/{courseId}`
- 결과:
  - backend test 통과
  - frontend lint/typecheck/build 통과
  - health/profile 라우트 정상 응답(200)
  - 카카오 authorize 단계에서 `KOE004` 재현(카카오 콘솔 설정 이슈로 확인)
  - 라이딩 생성 -> 코스 생성(from-riding) -> 코스 상세 조회까지 정상 동작 확인

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- 카카오 로그인 클릭 시 에러는 코드 이슈 외에 카카오 개발자 콘솔 설정 상태에 직접 영향받음.
  - 카카오 로그인 ON
  - Web 도메인 `http://localhost:3000`
  - Redirect URI `http://localhost:3000/auth/kakao/callback`
- 작업 트리에 본 작업과 직접 무관한 미커밋 변경(`OMX_GUIDE.md`, `backend/build.gradle`, `backend/docker-compose.local.yml`, 기타 docs/png)이 남아 있으므로 다음 커밋에서 분리 관리 필요.

## 7. 다음 작업(Next)

- 실제 카카오 계정으로 로그인 E2E 재검증(콘솔 설정 반영 후)
- 코스 상세 상단-지도 레이아웃 이슈(사용자 스크린샷 기준) 재현 환경 통일 후 UI 픽셀 단위 보정
- 필요 시 from-riding API에 대한 자동화 테스트 케이스 추가

# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-11 16:26:25 (KST)
- 작업명: AWS 홈테스트용 EC2 프록시 경로 추가
- 브랜치: master
- 범위(커밋): cfdc734..bfee704

## 1. 이번 작업의 목표

- AWS 크레딧을 활용해 외부 테스터가 집에서 빠르게 접속 가능한 배포 경로를 만든다.
- Vercel 웹과 EC2 backend를 무리 없이 연결할 수 있도록 프록시 경로와 환경변수 구조를 정리한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - EC2 단일 서버에서 backend + PostGIS를 함께 띄우는 홈테스트용 Docker Compose 초안 추가
- Mobile:
  - 직접 변경 없음
- Docs:
  - AWS EC2 홈테스트 배포 가이드 추가
  - docs 인덱스에 AWS 배포 가이드 연결
- Frontend:
  - Vercel에서 `/backend/*`를 EC2 backend로 프록시할 수 있도록 Next.js rewrite 추가
  - server-side route가 `INTERNAL_API_BASE_URL`을 우선 사용하도록 수정
  - `.env.example`에 프록시/내부 API 관련 예시값 추가

## 3. 변경된 API / 정책(있으면)

- 공개 API 스펙 변경 없음
- 프론트 배포 환경변수 추가:
  - `INTERNAL_API_BASE_URL`
  - `BACKEND_PROXY_ORIGIN`
- 배포 정책:
  - 홈테스트 경로는 `Vercel 웹 + EC2(backend + PostGIS)` 조합을 우선 사용

## 4. 검증(Verification)

- 실행한 커맨드:
  - `git diff --check`
  - `cd frontend && npm run lint`
  - `cd frontend && npm run typecheck`
- 결과:
  - 형식 오류 없음
  - 프론트 lint 통과
  - 프론트 타입체크 통과

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식(권장): Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 55%

## 6. 리스크/이슈/메모

- EC2 공개 IP가 바뀌면 Vercel/Kakao 설정값을 다시 맞춰야 한다.
- 홈테스트 편의를 위해 backend `8080`을 외부 개방하는 구성이므로 장기 운영용으로는 부적절하다.
- 현재 경로는 빠른 검증용이며, 운영 전환 시 HTTPS/API Gateway/App Runner/ALB 구조 재검토가 필요하다.

## 7. 다음 작업(Next)

- EC2 1대를 생성한 뒤 public IP 기준으로 Vercel env와 Kakao redirect URI를 최종 고정한다.
- EC2 초기 세팅 명령을 더 줄여 실제 의뢰자용 체크리스트를 1~2단계 수준으로 압축한다.
- 필요 시 EC2 접속 후 Docker, Compose, 서비스 기동 순서를 스크립트화한다.

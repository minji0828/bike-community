# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-07 01:15:00 (KST)
- 작업명: 프로필 hydration 불일치 수정
- 브랜치: master
- 범위(커밋): 2063134..(next)

## 1. 이번 작업의 목표

- `/profile` 진입 시 hydration mismatch 경고를 제거한다.
- SSR된 Client Component가 브라우저 전용 값(`window.location.search`)에 의존하지 않도록 조정한다.

## 2. 완료된 작업(Deliverables)

- Web:
  - `frontend/app/profile/page.tsx`
    - 초기 렌더 단계에서 `window.location.search`를 읽지 않도록 수정
    - mount 이후 비동기 effect에서만 `loginError` query를 읽어 상태 반영하도록 변경
  - `frontend/app/course/[id]/page.tsx`
    - 기본 모임 시작 시간 계산을 `Date.now()` 직접 합산 방식에서 보다 안정적인 날짜 조정 방식으로 정리

## 3. 변경된 API / 정책(있으면)

- 없음

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd frontend && npm run check`
- 결과:
  - frontend lint/typecheck/build 통과
  - `/profile` 포함 전체 Next build 성공

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- hydration mismatch의 직접 원인은 `profile/page.tsx` 초기 state initializer에서 `window.location.search`를 읽던 부분이었다.
- locale/time 기반 렌더링이 더 남아 있는 화면은 추후 또 경고가 날 수 있으므로 필요 시 같은 패턴으로 정리해야 한다.

## 7. 다음 작업(Next)

- 실제 브라우저에서 `/profile` 재진입 후 hydration 경고가 사라졌는지 확인
- 필요 시 채팅/코스 상세 페이지의 locale 기반 날짜 렌더도 추가 점검

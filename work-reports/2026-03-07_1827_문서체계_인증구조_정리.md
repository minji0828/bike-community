# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-07 18:27:23 (KST)
- 작업명: 문서 체계 정리, 로컬 재현성 보강, 인증 구조 리팩터링 기준선 정비
- 브랜치: master
- 범위(커밋): aea5527..b9e9226

## 1. 이번 작업의 목표

- 문서가 많아지면서 기준 문서와 참고 문서가 섞인 상태를 정리한다.
- 다른 PC에서도 동일하게 로컬 실행이 가능하도록 실행 기준 문서를 보강한다.
- 프론트/백엔드 인증 구조에서 중복과 결합도를 낮출 수 있는 저비용 리팩터링을 적용한다.
- Phase 1(Trip Collection + Highlights) 실행 계획을 별도 문서로 고정한다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - `AuthenticatedUserResolver` 추가로 JWT/Principal 사용자 ID 파싱 로직 공통화
  - `AuthController`, `LegacyAuthController`, `CourseController`, `CourseCommentController`, `CourseMeetupController`, `CourseMeetupChatController`, `LocationController` 중복 인증 로직 제거
  - WebSocket origin 허용 대상을 설정값 기반으로 변경
  - resolver 단위 테스트 추가: `AuthenticatedUserResolverTest`
- Web/Frontend:
  - `apiFetch` / `appRouteFetch` 계층 분리
  - `auth-provider`에 `status(checking/authenticated/anonymous/error)` 상태 모델 도입
  - 저장 토큰 bootstrap 로직을 `getStoredAuthState()`로 정리
  - 다른 PC/캐시 꼬임 대응용 `npm run dev:clean` 스크립트 추가
- Docs:
  - 루트 `README.md` 추가
  - `docs/README.md`를 실행/배포/전략 인덱스로 재구성
  - `docs/repository-audit-2026-03-07.md` 추가
  - `docs/local-dev-runbook.md`를 새 PC 기준 런북으로 보강
  - `backend/LOCAL_DEV.md`를 백엔드 보조 문서로 정리
  - `설계/README.md` 읽기 순서/분류 보강
  - `설계/27_Phase1_TripCollection_Highlights_실행계획.md` 추가

## 3. 변경된 API / 정책(있으면)

- API 경로/응답 스키마 변경 없음
- 설정 변경:
  - backend env: `APP_WEBSOCKET_ALLOWED_ORIGIN_PATTERNS` 추가
- 개발 실행 변경:
  - frontend script: `npm run dev:clean` 추가
- 문서 기준 변경:
  - 저장소 진입점은 `README.md`
  - 실행 기준은 `docs/local-dev-runbook.md`
  - 설계 기준은 `설계/README.md`

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd backend && ./gradlew test --no-daemon --console=plain`
  - `cd frontend && npm run check`
  - `curl http://localhost:8080/api/v1/health`
  - `curl http://localhost:3000/api/health`
  - `curl -I http://localhost:3000/profile`
  - `curl -I http://localhost:3000/meetups/1/chat`
  - `curl -i http://localhost:8080/api/auth/me`
  - `curl -i http://localhost:3000/api/auth/me`
- 결과:
  - backend test 통과
  - frontend lint/typecheck/build 통과
  - backend/frontend health 정상 응답
  - `/profile`, `/meetups/1/chat` 라우트 200 확인
  - 무인증 `auth/me`는 backend/frontend 모두 401 응답 확인

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식(권장): Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- 이번 작업은 대규모 구조 변경보다 “기준선 정리”에 집중했다.
- 여전히 큰 구조 리팩터링 후보는 남아 있다.
  - `CourseService` 분리
  - 채팅용 hook/state machine 정리
  - 컨트롤러/보안 경계 테스트 확충
- 문서 정리는 삭제보다 역할 분리 방식으로 진행했다. 기존 참고 문서는 결정 이력으로 유지했다.

## 7. 다음 작업(Next)

- `설계/27_Phase1_TripCollection_Highlights_실행계획.md` 기준으로 Collection/Highlight 정책과 API 문서를 구체화한다.
- 백엔드 Collection/Highlight 데이터 모델과 CRUD 경계를 먼저 설계한다.
- 이후 프론트 코스 상세/컬렉션 UI를 최소 범위로 구현한다.

# 작업 일지 / 결과 보고서

- 작성일시: 2026-01-04 16:39 (KST)
- 작업명: 초기 백엔드 베이스라인 구축(POI/라이딩/유저/위치/Swagger)
- 브랜치: master
- 범위(커밋): (root) dc48b33..9e4b25c

## 1. 이번 작업의 목표

- Spring Boot 기반의 서버 골격을 만들고, "지도 앱" MVP에 필요한 최소 데이터 파이프라인(POI/라이딩/위치)을 붙인다.
- API 응답 포맷/예외 처리/Swagger 문서를 갖춘 개발 베이스라인을 만든다.

## 2. 완료된 작업(Deliverables)

- Backend:
  - 프로젝트/빌드 베이스라인(Gradle wrapper, 기본 설정) 추가
  - POI(서울 공공 화장실) 동기화 + 조회 API 추가
    - full refresh / incremental / auto sync
    - 주변 조회 / 경로 주변 조회
  - 라이딩(riding) 저장 API 추가
  - 사용자 위치(Location) 조회/업데이트/이력 조회 API 추가
  - 공통:
    - Swagger 설정
    - `ApiResponse` 공통 응답
    - `BusinessException` + 글로벌 예외 처리
    - Geometry 유틸 추가
  - Infra:
    - Seoul OpenAPI client 추가
    - Kakao map client 추가

## 3. 변경된 API / 정책(있으면)

- POI:
  - `POST /api/v1/pois/sync/full`
  - `POST /api/v1/pois/sync/incremental`
  - `POST /api/v1/pois/sync/toilets`
  - `GET /api/v1/pois/nearby`
  - `POST /api/v1/pois/along-route`
- Riding:
  - `POST /api/v1/ridings`
- Location:
  - `GET /api/v1/locations/{userId}/current`
  - `POST /api/v1/locations/{userId}`
  - `GET /api/v1/locations/{userId}/history`
  - `GET /api/v1/locations/{userId}/period`
  - `GET /api/v1/locations/{userId}/recent`
  - `GET /api/v1/locations/nearby`
  - `GET /api/v1/locations/{userId}/distance`
  - `DELETE /api/v1/locations/{userId}/old-history`

## 4. 검증(Verification)

- 당시 실행 로그는 커밋에 남지 않아 확인 불가
- 단, 기본 테스트 클래스(`ProjectApplicationTests`)가 추가됨

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0

현황(해당 시점 기준, 사후 정리):

- M0 서버 베이스라인: Done
- M1 Course Catalog: Todo
- M2 Featured 운영: Todo
- M3 RN 최소 기능: Todo
- M4 UGC 코스 생성(from-riding) + GPS 문서: Todo
- M5 Backoffice 최소 API: Todo
- M6 인증 + 코스 댓글(MVP2): Todo
- M7 코스 모임 + 임시 채팅(MVP3): Todo

진척도(%):

- Done 1개 = 1
- 1 / 8 = 12.5%  -> 13%

## 6. 리스크/이슈/메모

- 사용자(User) API는 컨트롤러만 존재하고 엔드포인트는 비어 있음(초기 골격 단계)
- Location API는 운영/프라이버시 정책이 아직 고정되지 않았으므로, 실제 제품 반영 시 정책 문서와 정합성 점검 필요

## 7. 다음 작업(Next)

- Course Catalog(코스 생성/조회/공유) 도메인(M1) 구현
- 모바일 앱(M3) 착수(지도 + 코스 카드 + 따라가기)

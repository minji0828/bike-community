# 작업 일지 / 결과 보고서

- 작성일시: 2026-03-05 16:17:50 (KST)
- 작업명: 프론트 위치 API 연동 보강 및 로컬 실행(QR) 점검
- 브랜치: master
- 범위(커밋): 82cacd0..(working)

## 1. 이번 작업의 목표

- 백엔드 위치 API(`/api/v1/locations/**`)가 프론트 흐름에 실제로 연결되도록 보강한다.
- 로컬에서 백엔드/프론트 실행 상태를 점검하고 Expo QR 실행 가능 여부를 확인한다.

## 2. 완료된 작업(Deliverables)

- Mobile:
  - `src/types/bikeoasis.ts`
    - `LocationUpdateRequest`, `LocationRecord` 타입 추가
  - `src/api/bikeoasis.ts`
    - `updateUserLocation(userId, request)` 추가
    - `getCurrentUserLocation(userId)` 추가
  - `src/screens/RideScreen.tsx`
    - 주행 중 위치를 10초 간격으로 `POST /api/v1/locations/{userId}`로 best-effort 동기화
    - 로그인 토큰/유저ID 없으면 비활성 처리
    - UI에 "위치 동기화 상태" 표시(off/syncing/ok/error)
  - `src/screens/SettingsScreen.tsx`
    - 카카오 토큰 교환 성공 시 JWT `sub`를 파싱해 `userId` 자동 반영(가능한 경우)

## 3. 변경된 API / 정책(있으면)

- 백엔드 API 계약 변경 없음
- 프론트-백엔드 연결 강화:
  - Ride 화면에서 위치 업데이트 API를 실제 호출하도록 변경

## 4. 검증(Verification)

- 실행한 커맨드:
  - `cd frontend/mobile && npm run typecheck`
  - `cd frontend/mobile && npm run build:web`
  - `cd frontend/mobile && npx expo start --tunnel` (실행 확인)
  - `cd backend && ./gradlew bootRun` (실행 확인)
- 결과:
  - frontend typecheck: PASS
  - frontend build:web: FAIL
    - `Cannot find module '../lightningcss.linux-x64-gnu.node'`
  - expo tunnel: PASS (QR/exp URL 출력 확인)
  - backend bootRun: FAIL
    - `Connection to localhost:5432 refused` (로컬 PostgreSQL 미기동)

## 5. 마일스톤 진척도(설계/14 기준)

- 기준 문서: `설계/14_개발계획_마일스톤_체크리스트.md`
- 산출 방식: Done=1, Partial=0.5, Todo=0
- 이번 보고 시점 진척도: 87.5%

## 6. 리스크/이슈/메모

- 모바일 QR 실행은 가능하나, 백엔드 DB(PostgreSQL/PostGIS) 미기동 시 API 연동 검증은 제한된다.
- 웹 빌드(lightningcss native) 문제는 현재 Node/환경 조합 이슈로 보이며 CI(Node20)와 로컬 환경 차이가 있다.

## 7. 다음 작업(Next)

- 로컬 PostgreSQL(PostGIS) 기동 후 `bootRun` 재검증
- 모바일 설정에서 `apiBaseUrl`을 PC LAN IP로 맞춰 실제 단말 API 호출 확인
- 위치 history/current 조회 UI(또는 디버그 버튼) 추가 여부 결정
